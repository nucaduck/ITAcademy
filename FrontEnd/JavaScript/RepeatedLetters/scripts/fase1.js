//Crea un llistat amb el teu nom on cada posició correspongui a una lletra.
letters();

function letters(){
    var name = ['X','a','v','i','e','r'];
    var i = 0;
    var txt;
    while( i < name.length){
       txt = console.log(name[i]);
      i++;
    }
    //console.log(name);
   
}